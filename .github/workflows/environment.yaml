name: Environment level Practice
on: 
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    environment: prod
    steps:
      - name: prod
        run: |
          echo "Deploying to ${{vars.APP_URL}}"
